{
  "name": "youtube-kid-limiter",
  "version": "1.0.1",
  "description": "Chrome extension that limits kids' YouTube time with a brain-energy meter and restful breaks.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "build:store": "node scripts/build-store.mjs",
    "build:installer": "node scripts/build-installer.mjs",
    "build": "npm run build:store && npm run build:installer"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "adm-zip": "^0.5.16"
  }
}
